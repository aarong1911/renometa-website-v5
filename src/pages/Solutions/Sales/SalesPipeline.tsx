
import React from 'react';
import SolutionPageTemplate from '../SolutionPageTemplate';

const SalesPipeline = () => {
  return (
    <SolutionPageTemplate
      title="Sales Pipeline"
      subheadline="Track the progress of potential customers"
      category="Sales"
      categoryPath="sales"
    />
  );
};

export default SalesPipeline;
